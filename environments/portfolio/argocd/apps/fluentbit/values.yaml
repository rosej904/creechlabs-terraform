# Essential filters to make logs searchable in Grafana
config:
  filters: |
    [FILTER]
        Name                  multiline
        Match                 kube.*
        Multiline.Key_Content log
        Multiline.Parser      go, java, python, multiline_json

    [FILTER]
        Name                kubernetes
        Match               kube.*
        Kube_CA_File        /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        Kube_Token_File     /var/run/secrets/kubernetes.io/serviceaccount/token
        Kube_Tag_Prefix     kube.var.log.containers.
        Merge_Log           On
        Keep_Log            Off
        K8S-Logging.Parser  On
        K8S-Logging.Exclude Off

  # Output to Loki service
  outputs: |
    [OUTPUT]
        Name        loki
        Match       *
        Host        loki.observability.svc.cluster.local
        Port        3100
        Labels      job=fluent-bit
        Auto_Kubernetes_Labels on
        # For a homelab, buffer in memory to keep it simple
        line_format json
        log_level   info

testFramework:
  enabled: false

rbac:
  create: true

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi